These migrations were removed from `supabase/migrations/` because they use
nonconforming filenames and belong to deprecated Composer-era work.

They are preserved here for historical reference only and are not part of the
active migration chain.
