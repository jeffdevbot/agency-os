# Session Prompts Archive

These files were temporary restart prompts used during active build/debug
sessions.

They are kept for historical context, but they are not the primary entrypoint
for current work. Prefer the live handoff and status docs in the top-level
`docs/` directory instead.
